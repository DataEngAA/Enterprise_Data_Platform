# Monitoring and Observability

Infrastructure: CPU, memory, disk, Lambda failures, ECS failures, SQS depth/age, DLQ count, DMS lag, Kinesis iterator age, API latency.

Data: freshness, volume, schema changes, nulls, duplicates, quarantine rate, reconciliation, and SLA compliance.
