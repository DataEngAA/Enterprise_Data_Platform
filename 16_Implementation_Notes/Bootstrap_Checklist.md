# Bootstrap Checklist

See `02_Infrastructure/Terraform_Bootstrap_Design.md` for the full design (state lifecycle, locking, roles, backend mechanics, module structure) behind the Terraform-related items below, and `16_Implementation_Notes/Terraform_Bootstrap_Implementation_Plan.md` for the file-by-file build plan. Design and plan only — no item below is implemented yet except where marked.

- [x] Choose AWS region — `ap-south-1` (Mumbai), verified (`02_Infrastructure/AWS_Account_Preparation.md`).
- [x] **Configure secure AWS access — CONFIRMED COMPLETE (2026-08-04).** Root/IAM user MFA verified; IAM instance profile, workstation role, and deployment role all created, deployed, and validated in real AWS (bootstrap IAM policies, Bootstrap Update 2's trust policy, and a final `aws ec2 run-instances --dry-run` success under the shared deployment role — see items below and `PROJECT_EXECUTION_JOURNAL.md` Section 27ak).
- [x] Create GitHub repository — exists.
- [x] Terraform Bootstrap **design** documented and **reviewed/approved by user** (2026-07-24) — `02_Infrastructure/Terraform_Bootstrap_Design.md`. Bootstrap state itself is **not yet created**.
- [x] Terraform Bootstrap **implementation plan** documented and **REVIEWED/APPROVED by user (2026-07-25)** — `16_Implementation_Notes/Terraform_Bootstrap_Implementation_Plan.md`. Approved first scope: `bootstrap/` only (ten files, no modules — state bucket + hardening and the deployment role defined directly in `bootstrap/main.tf`; `Environment = shared` tagging). **A separate, explicit authorization is still required before any `terraform` command (`init`/`plan`/`apply`) is run** — plan approval covers file creation only.
- [x] `bootstrap/` Terraform files **created** (2026-07-25) — `versions.tf`, `providers.tf`, `backend.tf`, `variables.tf`, `locals.tf`, `main.tf`, `outputs.tf`, `terraform.tfvars.example`, `backend.hcl.example`, `README.md`, all under `infrastructure/terraform/bootstrap/`. State bucket hardening and the deployment role (with a deliberately narrow, explicitly-labeled placeholder permissions policy) are defined directly in `main.tf`, no modules.
- [x] `bootstrap/` Terraform code **static-review corrections applied** (2026-07-25) — AWS provider constraint corrected to `>= 6.0.0, < 7.0.0`; removed the no-op `bucket_key_enabled` from the SSE-S3 config; `human_bootstrap_principal_arn` trust-policy validation narrowed to IAM-user ARNs only (Identity Center/STS assumed-role support documented as a deferred, separate future design); added required `aws_account_id` variable wired into `providers.tf`'s `allowed_account_ids` safety check; added `bootstrap_state_key` variable and rewrote the deployment role's permission policy to grant `ListBucket` (prefix-conditioned), bucket metadata reads, `GetObject`/`PutObject` (no delete) on the exact state object, and `GetObject`/`PutObject`/`DeleteObject` only on the exact `.tflock` lock object — no more bucket-wide or `DeleteObject`-on-state-object access; fixed `locals.tf` tag-merge ordering so `additional_tags` cannot override required tags; `backend.hcl.example` now includes `encrypt = true` and an explicit key-must-match-`bootstrap_state_key` note; `README.md` updated accordingly (prerequisites, required inputs, unresolved-permission-scope, import guidance marked illustrative).
- [x] `bootstrap/` Terraform code **final static-review corrections applied** (2026-07-25) — `README.md` local-bootstrap command order fixed (`init` before `validate`, not after; `init` explained as using the local backend since `backend.tf`'s `s3` block is still commented out); added a `lifecycle.precondition` on `aws_iam_role.deployment` requiring `human_bootstrap_principal_arn`'s account to equal `aws_account_id`, same `lifecycle` block as `prevent_destroy`; **deployment-role permission model changed** — `aws_iam_policy.deployment_permissions`, `aws_iam_role_policy_attachment.deployment_permissions`, and their policy document were **removed entirely** (bootstrap remains human-administered; the deployment role is for the future `environments/dev`, not this bootstrap's own state, so it must not access `bootstrap/terraform.tfstate`); `aws_iam_role.deployment` now exists with **no permissions policy attached at all** (not `AdministratorAccess`, not a placeholder wildcard), with a documented future-change plan (exact dev state/lock-object access + approved VPC/IAM/EC2 permissions); documentation-accuracy fixes to `allowed_account_ids` wording (STS-check-then-prevent, not "aborts before any API call"), explicit hardening-resource list replacing "five hardening resources," and updated `terraform import` guidance. **No `terraform` command has been run against these files** — `init`/`fmt`/`validate`/`plan`/`apply`/`tflint`/`tfsec` are all still pending, and `.terraform.lock.hcl` does not exist. Next task: controlled local formatting and initialization review.
- [x] Terraform installed/available on the local machine used to run it — confirmed (2026-07-25) by a successful `terraform version` (superseding the earlier Cowork-sandbox blocker, which had no Terraform binary and no reachable distribution source; see the prior entry below for that historical record).
- [x] `terraform fmt -recursive` — **succeeded (2026-07-25)**, run from `infrastructure/terraform/bootstrap/`; reformatted `variables.tf` (style only, no semantic change).
- [x] `terraform fmt -check -recursive` — **succeeded (2026-07-25)** with no output, confirming the tree is fully formatted after the `fmt -recursive` pass above.
- [x] Local provider initialization via `terraform init -backend=false` — **succeeded (2026-07-25)**. The remote S3 backend was **not** configured or accessed (`backend.tf`'s `s3` block remains commented out). AWS provider **hashicorp/aws v6.56.0** was installed, satisfying `versions.tf`'s `>= 6.0.0, < 7.0.0` constraint.
- [x] `.terraform.lock.hcl` generation — **generated (2026-07-25)**, recording `hashicorp/aws` `6.56.0`; must be retained and committed to version control, never hand-edited.
- [x] `terraform validate` — **succeeded (2026-07-25)**: `Success! The configuration is valid.`
- [x] `tflint --init` — **succeeded (2026-07-25)**, TFLint version 0.64.0, all plugins already installed.
- [x] `tflint` (lint run) — **completed (2026-07-25) with zero findings.** Initial run reported one fixable warning (`variable "bootstrap_state_key" is declared but not used"`, rule `terraform_unused_declarations`, `variables.tf` line 133); corrected by removing the unused `bootstrap_state_key` variable from `variables.tf` (no replacement unused variable added), removing its commented-out override from `terraform.tfvars.example`, and updating `backend.hcl.example`/`README.md` so the state key is documented as the approved bootstrap state key supplied via `backend.hcl` rather than a Terraform variable to "match." Rerun confirmed the warning is cleared.
- [x] Initial Trivy security scan — **completed (2026-07-25)**, Trivy 0.72.0, `trivy config --misconfig-scanners=terraform .`. Not a clean/zero-findings scan: **two findings reviewed and accepted as Phase 0 design exceptions** — AWS-0089 (LOW, S3 access logging disabled, deferred to a future monitoring/hardening phase) and AWS-0132 (HIGH, no customer-managed KMS key, SSE-S3/AES256 retained deliberately). Full rationale, residual risk, and reconsideration triggers recorded in `infrastructure/terraform/bootstrap/README.md` "Security Scan Evidence." Neither finding was suppressed in code (no `.trivyignore`, no inline ignore annotation).
- [x] Input-aware final Trivy security scan — **completed (2026-07-25)**, `trivy config --misconfig-scanners=terraform --tf-vars terraform.tfvars .`, run against a real, gitignored `terraform.tfvars`. Variable values were successfully loaded; the prior missing-variable warning did not appear; no parser errors occurred; **no new findings appeared**. The same two findings remain — AWS-0089 (LOW) and AWS-0132 (HIGH) — **still reviewed and accepted as Phase 0 design exceptions, not fixed defects.** This is **not** described as a clean/zero-findings scan. Full detail in `infrastructure/terraform/bootstrap/README.md` "Input-Aware Security Scan Rerun."
- [x] **Pre-`plan` validation/security gate — complete (2026-07-25).** `terraform fmt`/`init -backend=false`/`validate` (tier 4), TFLint (zero findings), and the input-aware Trivy rerun (two accepted findings, no new findings) have all completed. `tfsec`/`checkov` remain unrun but are not part of this gate as tracked. This completion does **not** authorize `terraform plan`/`apply`, AWS resource creation, backend migration, or locking verification — each remains a separate, later, explicit authorization.
- [~] `tfsec`/`checkov` — not yet run.
- [x] Real input preparation — a real, gitignored `terraform.tfvars` now exists on the machine used for the Terraform/TFLint/Trivy runs (values not reproduced in this repository). `backend.hcl` (real values) is **not** created — not needed until the future state-migration step.
- [x] Break-glass procedure **documented (2026-07-25)** — `16_Implementation_Notes/Terraform_Bootstrap_Break_Glass_Procedure.md`. Covers normal vs. emergency access, deployment-role trust-policy recovery, lost bootstrap-operator access, state-bucket/state-file inaccessibility, stale native S3 `.tflock` recovery (force-unlock vs. direct deletion, both gated), `terraform import`, safe `prevent_destroy` handling, root's emergency-only role, and evidence-capture/post-incident-review requirements. No second IAM identity, ARN, account ID, or named emergency operator was invented; unresolved ownership items are marked "Operational owner confirmation pending."
- [ ] Break-glass procedure **testing/drill** — **not done.** The procedure above has not been exercised against any real incident or AWS resource; this remains "Validation evidence pending" until an actual drill or incident occurs.
- [x] Project execution journal **created (2026-07-25)** — `16_Implementation_Notes/PROJECT_EXECUTION_JOURNAL.md`, the authoritative chronological process history (design decisions, static-review corrections, the bootstrap-management-model decision, the Cowork tooling blocker and its resolution, full command history, mistakes-and-corrections index, resume-from-here checklist). **Documentation only — creating this file does not itself complete, validate, or advance any technical item on this checklist**; every item above/below reflects its own actual status, not the existence of a journal entry describing it.
- [x] `terraform plan` — **run and succeeded (2026-07-25)**: `terraform plan -out "bootstrap.tfplan"` from `infrastructure/terraform/bootstrap/` on the local Windows machine. Summary: `Plan: 7 to add, 0 to change, 0 to destroy.` Seven resources proposed — `aws_iam_role.deployment`, `aws_s3_bucket.terraform_state`, `aws_s3_bucket_ownership_controls.terraform_state`, `aws_s3_bucket_policy.terraform_state`, `aws_s3_bucket_public_access_block.terraform_state`, `aws_s3_bucket_server_side_encryption_configuration.terraform_state`, `aws_s3_bucket_versioning.terraform_state` — matching the approved bootstrap scope exactly, no unexpected destroy/replace. An earlier command form, `terraform plan -out=bootstrap.tfplan`, failed with `Too many command line arguments.`; the corrected form above succeeded — a syntax error, not a configuration defect. Full manual-review detail in `infrastructure/terraform/bootstrap/README.md` "Terraform Plan Results." Saved plan file `bootstrap.tfplan` is a sensitive local binary artifact — already `.gitignore`d, must remain local, unshared, uncommitted; its contents were not inspected or reproduced here.
- [x] **Plan-review gate — complete (2026-07-25).** The saved plan was manually reviewed in full: region, tags (`Environment = shared` plus the other required tags), S3 hardening (SSE-S3/AES256, versioning enabled, `BucketOwnerEnforced`, all four Block Public Access settings `true`, TLS-only bucket policy), deployment role (correct name, trust principal, MFA condition, 1-hour max session duration, no permissions policy attached), and the absence of any VPC/EC2/NAT/KMS/DynamoDB/Lambda/Budget/CI-CD/test/stage/prod resource. This completion does **not** authorize `terraform apply`, AWS resource creation, AWS-side verification, remote backend migration, or native-locking verification — each remains a separate, later, explicit authorization.
- [x] `terraform apply` — **run and succeeded (2026-07-25)**: `terraform apply "bootstrap.tfplan"` from `infrastructure/terraform/bootstrap/` on the local Windows machine, applying the exact reviewed, saved plan. Result: `Apply complete! Resources: 7 added, 0 changed, 0 destroyed.` All seven resources were created by Terraform, not manually in the AWS Console. This is the first AWS resource this project has ever created.
- [x] **AWS operational verification — complete (2026-07-25)** for the current state-bucket and deployment-role controls. Verified via AWS CLI (`--no-cli-pager` added after output paging was encountered): state bucket versioning `Enabled`; server-side encryption `AES256`/SSE-S3; all four Block Public Access settings (`BlockPublicAcls`, `IgnorePublicAcls`, `BlockPublicPolicy`, `RestrictPublicBuckets`) `true`; object ownership `BucketOwnerEnforced`; bucket policy contains an explicit `Deny` for `s3:*` when `aws:SecureTransport` is `false`. Deployment role name `enterprise-data-platform-shared-deployment-role`; max session duration `3600` seconds; trust principal the intended IAM user; `aws:MultiFactorAuthPresent = true` condition present; **no attached managed policies; no inline policies.** Full detail in `infrastructure/terraform/bootstrap/README.md` "Terraform Apply Results and AWS Verification." Account ID, IAM username, bucket name, and ARNs are not reproduced in any documentation.
- [x] **Bootstrap deployment gate — complete (2026-07-25).** Deployment evidence (tier 6) obtained: `terraform apply` ran against real AWS and created exactly the seven reviewed resources. Operational-verification evidence (tier 7) obtained for the specific controls checked above. This completion does **not** cover remote backend migration, native S3 locking verification, a no-change `terraform plan` against the now-real infrastructure, break-glass drilling, or any `environments/dev` work — each remains separately incomplete.
- [x] **No-change `terraform plan` against the now-real infrastructure — run and succeeded (2026-07-25)**: `0 to add, 0 to change, 0 to destroy.`, run as part of post-migration verification (see below). Confirms migrated remote state, the configuration, and the real, already-applied AWS resources all agree with nothing left to reconcile.
- [x] **Backend-migration preparation — complete (2026-07-25).** `infrastructure/terraform/bootstrap/backend.tf`'s partial `backend "s3" {}` block has been **activated (uncommented)** — no bucket name, account ID, role ARN, credentials, or local path hardcoded; no `assume_role` configuration; no DynamoDB configuration; backend settings still supplied only via `backend.hcl` at `terraform init` time. `backend.hcl.example` reviewed and confirmed to already contain exactly the five approved settings (`bucket = "<STATE_BUCKET_NAME>"`, `key = "bootstrap/terraform.tfstate"`, `region = "ap-south-1"`, `encrypt = true`, `use_lockfile = true`) with no credentials/access keys/profile/deployment-role ARN/DynamoDB/KMS/real values — no change needed. `.gitignore` reviewed and confirmed correct (`backend.hcl`, `terraform.tfstate`/`.tfstate.*`, `*.tfplan`, `.terraform/`, `terraform.tfvars` ignored; `backend.hcl.example`, `terraform.tfvars.example`, `.terraform.lock.hcl`, `backend.tf` commit-eligible) — no change needed. `README.md`'s "Future State Migration Sequence" rewritten into the full a-n gated procedure plus a new "State Safety" section.
- [x] **Real `backend.hcl` created (by the user, with the real bucket name) — done.** Implied by the successful migration below, which requires `-backend-config="backend.hcl"` to resolve to a real, valid backend configuration. Contents not reproduced in this repository (gitignored).
- [x] **Remote backend configuration activation via `terraform init -backend-config="backend.hcl" -migrate-state` — run and succeeded (2026-07-25).** Terraform reported `Successfully configured the backend "s3".` and `Terraform has been successfully initialized!`. AWS provider `hashicorp/aws` v6.56.0 was reused from `.terraform.lock.hcl` — no new download. No migration error occurred.
- [x] **State migration (`-migrate-state`) — complete (2026-07-25).** Local Terraform state has been migrated into the S3 backend. Full detail in `infrastructure/terraform/bootstrap/README.md` "State Migration Results and Post-Migration Verification."
- [x] **Remote-state readability — verified (2026-07-25).** `terraform state list` succeeded and returned all seven managed bootstrap resources, read from the new S3 backend. `aws s3api head-object` succeeded for `bootstrap/terraform.tfstate`, confirming the remote state object exists in the verified S3 state bucket. Bucket name, account ID, IAM username, ARNs, and state contents are not reproduced in any documentation.
- [x] **Remote-state migration gate — complete (2026-07-25).** Remote backend configuration, state migration, and remote-state readability are all confirmed with independently obtained evidence (`terraform init -backend-config -migrate-state` success, `terraform state list`, `terraform plan` showing `0/0/0`, `aws s3api head-object`). This completion does **not** cover native S3 locking verification or local-state/backup cleanup — both remain separately incomplete below. Deployment evidence (tier 6) and operational-verification evidence (tier 7) obtained for the seven bootstrap resources (apply + AWS CLI checks) are preserved unchanged.
- [~] **Native S3 locking — configured and passively exercised; deliberate contention test deferred.** `use_lockfile = true` is active (supplied via the real `backend.hcl` used for the successful migration) and every backend operation since migration (the migration itself, `terraform state list`, `terraform plan`) has succeeded without error. This is **passive evidence that the backend configuration works** — it is **not** claimed as verified concurrent-lock contention behavior. A deliberate, artificial contention test against this single-operator bootstrap configuration is not being staged; **explicit contention verification is deferred to the `environments/dev` Terraform phase**, where a genuinely longer-running operation (VPC/EC2 apply) gives a safer, more natural window to observe real lock contention. No `.tflock` object has been, or will be, manually created, deleted, or manipulated in the interim.
- [x] **Final remote-state readability and no-change check — run and succeeded (2026-07-25).** `terraform state list` succeeded (remote state remained readable; all seven managed bootstrap resources present); `terraform plan` succeeded (`No changes. Your infrastructure matches the configuration.` / `0 to add, 0 to change, 0 to destroy.`), run immediately before touching any local artifact. This is a recheck of, not a replacement for, the remote-state migration/readability evidence already obtained (`Remote-state readability — verified` above).
- [x] **Local Terraform state cleanup — complete (2026-07-25).** `Get-ChildItem .\terraform.tfstate*` before cleanup showed `terraform.tfstate` (0 bytes — the local placeholder left behind after backend migration) and `terraform.tfstate.backup` (19320 bytes — Terraform's own automatic local backup, containing the prior local state copy). `Remove-Item .\terraform.tfstate` succeeded; `Remove-Item .\terraform.tfstate.backup` succeeded. `Get-ChildItem .\terraform.tfstate*` after cleanup confirmed no local `terraform.tfstate` files remain in `infrastructure/terraform/bootstrap/`. Full detail in `infrastructure/terraform/bootstrap/README.md` "Final Remote-State Checks and Local State Cleanup." No state contents, account IDs, bucket names, IAM names, or ARNs reproduced anywhere.
- [x] **External temporary state backup deleted — complete (2026-07-25).** The temporary backup created per `README.md` "Future State Migration Sequence" step c (a distinct artifact from the local `terraform.tfstate.backup` file removed above) was confirmed to exist, deleted, and confirmed absent. Remote state had already been independently verified before this deletion. No S3 state object, `backend.hcl`, `terraform.tfvars`, `.terraform.lock.hcl`, or `.terraform/` content was touched; no AWS resource was changed. Full detail in `infrastructure/terraform/bootstrap/README.md` "External Temporary Backup Deletion and Bootstrap Phase Closeout." Path not reproduced; no state contents, account IDs, bucket names, IAM names, or ARNs reproduced anywhere.
- [x] **Bootstrap state cleanup — complete in full (2026-07-25).** Local placeholder (`terraform.tfstate`), local automatic backup (`terraform.tfstate.backup`), and the separate temporary external backup have all been removed. No local or external copy of bootstrap Terraform state remains anywhere; the S3 backend is the sole, authoritative state location.
- [x] ~~Controlled local `terraform fmt`/`init -backend=false`/`validate` — attempted and BLOCKED by Cowork-sandbox environment/tooling unavailability (2026-07-25)~~ — **historical record, superseded by the success above.** Pre-command safety checks passed (no active remote backend, `-backend=false` cannot migrate state, `validate` doesn't require real tfvars, no command creates AWS resources). Only `terraform version` had been run at that time, from `infrastructure/terraform/bootstrap/`, and failed with exit code 127 (`command not found`) — that Cowork sandbox had no Terraform binary, no root/package-manager access to install one, and every known Terraform/GitHub/conda/Docker distribution source was blocked by its network allowlist. Per the stop-on-failure rule, `fmt`/`init`/`validate` were deliberately not run at that time. Resolved by running the exact handoff sequence documented in `infrastructure/terraform/bootstrap/README.md` ("Terraform Tooling Blocker and Handoff") on a different machine with a working `terraform` binary — see the entries above for the successful result.
- [x] Bootstrap Terraform state (remote S3 backend + native locking) — **the state bucket exists in AWS (created by the 2026-07-25 apply) and state has been successfully migrated into it, with remote-state readability independently verified and reconfirmed (above), and all local and external Terraform state cleanup is complete.** Native S3 locking is configured and passively exercised; a deliberate contention test is deferred to the `environments/dev` Terraform phase (above), not performed here — this deferral does not block marking the state-management item itself complete.
- [x] **Bootstrap phase — FORMALLY COMPLETE (2026-07-25).** Completion summary: code created; validated (`fmt`/`init -backend=false`/`validate`, tier 4); linted (TFLint, zero findings after one correction); security-scanned (Trivy, twice, two accepted design exceptions both times, no new findings on rerun); planned (`terraform plan`, reviewed, tier 5); applied against real AWS with AWS-side verification (`terraform apply`, tiers 6-7); remote state migrated to S3 with independent post-migration verification (tier 7); a no-change `terraform plan` confirmed both immediately after migration and again as a final recheck before cleanup; all obsolete local and temporary state backups removed (local placeholder, local automatic backup, and the separate external backup). **Deliberately still pending, unaffected by this closeout:** native S3 locking contention testing (deferred to `environments/dev`), break-glass drilling (procedure documented, not tested), and `environments/dev` itself (does not exist). See `infrastructure/terraform/bootstrap/README.md` "Bootstrap Phase Completion Summary" for the full itemized closeout.
- [x] **`environments/dev` implementation plan — DRAFTED (2026-07-25), REVISED (2026-07-26), FINALIZED and REVIEWED/APPROVED BY USER (2026-07-26).** Zero blocking decisions remain (§56.1 empty): the complete deployment-role IAM policy (action/resource/condition matrix + full JSON, placeholders only), unrestricted outbound egress with documented trade-off, hardened AZ-selection strategy, `instance_metadata_tags = "disabled"`, and the full bootstrap-script scope specification are all finalized. All three flagged documentation conflicts are resolved, with `Terraform_Bootstrap_Design.md`, `Networking.md`, and `IAM_and_Access.md` each edited to match. **This approval authorizes only the next task — creating the plan's proposed files (§7) — not running any Terraform or AWS CLI command.** See prior entry below for full drafting/revision history. `16_Implementation_Notes/Dev_Environment_Terraform_Implementation_Plan.md` created, covering scope, directory tree, module justification (all six required boundary decisions **explicitly confirmed/approved** in the 2026-07-26 revision), file-by-file responsibility, dependency order, an explicit A-I stage-separation table plus a higher-level **three-stage IAM sequencing** (Stage A "Bootstrap update 1" → Stage B "`environments/dev` apply" → Stage C "Bootstrap update 2", added 2026-07-26, spelling out the deployment-role-permissions/deployment-role-trust/workstation-role-existence chicken-and-egg dependency explicitly), backend/state design (`dev/terraform.tfstate`; backend and provider each with their **own, corrected** `assume_role` block — a nested `assume_role = { role_arn, session_name }` object in `backend.hcl`, distinct from `providers.tf`'s own `assume_role` block, **not** a standalone `role_arn` field as the original draft incorrectly described), the deployment-role permission-update and trust-update descriptions (as future edits to *existing* `bootstrap/` files), workstation-role permissions, a proposed VPC/subnet CIDR scheme (flagged, **still not approved — blocking**), an AMI lookup **now resolved at the `environments/dev` root** rather than inside `modules/ec2-workstation` (revised 2026-07-26 — AMI selection is region/environment-specific and now visible in the root's own `plan`), a bootstrap-script path **relocated to `infrastructure/terraform/scripts/bootstrap_workstation.sh`** (inside `infrastructure/terraform/`, not a top-level repo directory), a new IMDSv2-enforcement proposal (flagged, not yet approved), the allowed EC2 instance-type set **finalized** (`t3.medium` default, `t3.large`, `t3.xlarge`), validation/plan/apply/AWS-verification criteria, the native-S3-lock contention-test mechanism (scheduled for this phase's own `apply`), partial-apply recovery/import guidance, open decisions now **explicitly split into blocking and non-blocking** (2026-07-26), and explicit exit criteria. Three documentation conflicts remain explicitly flagged, not silently resolved (single dev state key vs. `Terraform_Bootstrap_Design.md` §8; `Networking.md` vs. the approved minimal design; `IAM_and_Access.md`'s stale naming example). **This is a document only — no `.tf` file, Terraform directory, `backend.hcl`, `terraform.tfvars`, or `scripts/` file has been created; no Terraform or AWS CLI command has been run; no AWS resource created or modified.** **Two explicit blocking decisions remain before code creation can begin: (1) the exact deployment-role permissions policy JSON; (2) the VPC and public-subnet CIDR values.**
- [x] **Bootstrap Update 1 — CODE ADDED (2026-07-26), REVIEWED AND CORRECTED (2026-07-26), then APPLIED FOR REAL (2026-07-26, reported by the user).** `infrastructure/terraform/bootstrap/main.tf` defines `data.aws_iam_policy_document.deployment_dev_permissions` (**now SPLIT into THREE policy documents, 10 + 12 + 4 = 26 statements total, as of the 2026-07-26 second size-quota-failure correction below** — was two documents, 14 + 12, after the first size-quota-failure correction; a single 26-statement document before that; 18 before the second partial-apply-failure correction; 17 at original code-creation), `aws_iam_policy.deployment_dev_permissions` + `aws_iam_policy.deployment_dev_networking_permissions` + the new `aws_iam_policy.deployment_dev_workstation_iam_permissions`, and their three matching `aws_iam_role_policy_attachment` resources, plus a new `dev_workstation_role_name` variable (`variables.tf`). Every account-specific value uses a real Terraform reference (`var.aws_account_id`, `var.aws_region`, `aws_s3_bucket.terraform_state.arn`) — no placeholder string or hardcoded ARN. The role's trust policy is unchanged (human-MFA-only). **A review of the v1 policy (2026-07-26) found a real condition-scoping defect**: the single `DevRunInstances` statement applied `ec2:Owner = amazon` across a `Resource` list mixing the AMI resource type with five non-AMI resource types — corrected by splitting into `DevRunInstancesAmi` (AMI only, carries `ec2:Owner`) and `DevRunInstancesSupportingResources` (the five non-AMI types, no `ec2:Owner`); the pre-existing `DevRunInstancesTagOnCreate` statement is unchanged. `aws_iam_role.deployment`'s stale `NO PERMISSIONS ARE ATTACHED YET` description was also corrected the same day. **Bootstrap Update 1 was then applied for real** — the deployment role's dev-scoped policy is now attached in AWS, evidenced by the fact that the user was able to run a real, partially-successful `environments/dev` apply using it (see the first partial-apply-failure item below). **A second real-world gap was found the same way (via that partial apply, not static review)**: `DevReadOnlyDescribe` was missing `ec2:DescribeVpcAttribute`, corrected in source. **A third, structural gap was then found via a SECOND real partial apply**: `DevNetworkingCreateTaggedOnly` applied `aws:RequestTag` conditions to actions (`CreateSubnet`/`CreateRouteTable`/`CreateSecurityGroup`) that authorize against two resources per call — the new resource and the existing parent VPC — with no value for the existing-VPC side, causing a real `AccessDenied`; corrected by splitting into 8 statements (see the second partial-apply item below). **A fourth issue then surfaced not as a permissions gap but as a real IAM size-quota rejection**: attempting to apply the resulting 26-statement combined policy failed at `iam:CreatePolicyVersion` with `LimitExceeded: Cannot exceed quota for PolicySize: 6144` — no AWS change occurred, the prior 18-statement version stayed active. Corrected by splitting the single policy into two managed policies (see the size-quota-failure item below). **A fifth issue then surfaced from that split's own safeguard**: the resulting `deployment_dev_permissions` document's own `lifecycle.precondition` reported its rendered JSON at 6212 characters against the 6144 quota — caught entirely at plan time, no AWS command run, no AWS change occurred. Corrected by splitting the workstation-IAM statements out into a third managed policy (see the second size-quota-failure item below). **A sixth issue then surfaced from a real, reviewed `terraform plan` against that three-policy split, before any apply**: the plan showed `6 to add, 1 to change, 2 to destroy` — two unexpected destroys, caused by `aws_iam_policy.deployment_dev_permissions`'s `description` argument (a `Forces new resource` attribute in the AWS provider, since AWS IAM has no API to update a policy's description post-creation) having been rewritten by each of the prior two size-quota corrections, queuing a forced replacement of this already-live, real policy (and its attachment, cascading). Corrected by resetting `description` to a stable, split-narrative-free string and adding `lifecycle.ignore_changes = ["description"]`, permanently preventing this class of mistake on this resource (see the forced-replacement item below). **A seventh issue then surfaced from a real apply attempted against that fix**: the apply progressed further than any prior attempt — it detached and deleted the live `deployment_dev_permissions` policy, created and attached the two new policies, then failed to recreate `deployment_dev_permissions` with a real AWS `ValidationError` (`description` exceeded IAM's 1,000-character hard limit). Corrected by shortening all three policies' descriptions to short, stable strings. **An eighth, final issue was then closed pre-emptively**: `lifecycle.ignore_changes = [description]` was added to the remaining two policies (`deployment_dev_networking_permissions`, `deployment_dev_workstation_iam_permissions`), matching the pattern already on `deployment_dev_permissions`, eliminating this entire class of failure across all three resources. **Following these two corrections, Bootstrap Update 1 was applied for real, successfully, and a fresh `terraform plan` reported `No changes. Your infrastructure matches the configuration.` All eight corrections are now confirmed live in real AWS — see the new items below.**
- [x] Deployment-role permission update — **applied** (IAM Sequencing Stage A / Stage-Separation-table Stage A) — applied for real (reported by the user); the `ec2:DescribeVpcAttribute` and networking/tagging-restructuring follow-on corrections below have NOT yet been re-applied. **UPDATE (2026-07-26): superseded by full re-application below — see item "Bootstrap Update 1 — FINAL, STABLE RE-APPLICATION" further down. All follow-on corrections, including the eighth (final) one, are now confirmed live in real AWS.**
- [~] **First real `environments/dev terraform apply` — PARTIALLY SUCCEEDED THEN FAILED (2026-07-26, reported by the user).** Real AWS error: the shared deployment role was denied `ec2:DescribeVpcAttribute` while Terraform was creating the VPC and waiting for the `enable_dns_hostnames` attribute update to complete. Partial resources created in real AWS before the stop: the workstation IAM role, its `AmazonSSMManagedInstanceCore` attachment, its inline `AssumeRole` policy, its instance profile, and a VPC (creation began, a real VPC ID was returned). **Corrected**: `ec2:DescribeVpcAttribute` added to `DevReadOnlyDescribe` in `bootstrap/main.tf`.
- [~] **Second real `environments/dev terraform apply` — again PARTIALLY SUCCEEDED THEN FAILED (2026-07-26, reported by the user), after the first correction above was applied.** Created: a replacement VPC and an Internet Gateway. Already present from the first partial apply: the workstation IAM role, its `AmazonSSMManagedInstanceCore` attachment, its inline `AssumeRole` policy, its instance profile. Failed with `AccessDenied`: `ec2:CreateSubnet`, `ec2:CreateRouteTable`, `ec2:CreateSecurityGroup`, each against the parent VPC's ARN. **Root cause**: `DevNetworkingCreateTaggedOnly`'s single-statement `aws:RequestTag` condition had no value for the existing-parent-VPC side of these multi-resource-type actions. **Corrected**: `DevNetworkingCreateTaggedOnly` replaced with 8 statements (new-resource/existing-parent-VPC pairs, `aws:RequestTag`/`aws:ResourceTag` respectively) confirmed against a real 2026-07-26 AWS EC2 Service Authorization Reference fetch; `DevTagging` replaced with 2 narrower statements (create-time via `ec2:CreateAction`, pattern-inferred not independently confirmed; post-creation via `aws:ResourceTag`). Statement count now 26 (source only, not yet planned/applied). **No manual cleanup, state manipulation, or import was performed.** Neither `dev_initial.tfplan` nor `dev_recovery.tfplan` may be reused. **New, unresolved risk**: 10 net new statements were added; AWS's customer-managed-policy size quota (commonly 6,144 non-whitespace characters) was not independently, reliably measurable in this sandbox — must be checked for real via the next bootstrap plan/apply. Recovery proceeds via: validate the bootstrap correction → new Bootstrap Update 1 plan/apply (checking policy size) → `environments/dev` `terraform state list` → fresh `environments/dev` `terraform plan` → review whether the VPC/Internet Gateway/other partial resources reconcile or a replacement is proposed, specifically reviewing any tainted or partially created resource. No `environments/dev` apply is to be rerun until that fresh plan is reviewed. See `PROJECT_EXECUTION_JOURNAL.md` for the full incident record.
- [x] **Deployment-role managed-policy size-quota failure — REAL AWS REJECTION, SPLIT INTO TWO POLICIES (2026-07-26, reported by the user); RE-APPLIED FOR REAL AND CONFIRMED STABLE (2026-07-26, see final completion item below).** An attempt to apply the second correction's 26-statement combined policy failed at `iam:CreatePolicyVersion`: `LimitExceeded: Cannot exceed quota for PolicySize: 6144`. **No AWS change occurred** — a failed `CreatePolicyVersion` call leaves the existing default policy version active; this is a plan/apply-time API rejection, not a partial resource creation like the two prior incidents, and nothing required manual cleanup or state reconciliation. **Root cause**: the combined policy's rendered JSON exceeds AWS's 6,144-character customer-managed-policy size quota. **Corrected**: `bootstrap/main.tf`'s single `data.aws_iam_policy_document.deployment_dev_permissions` split into two documents/resources attached to the same role — `aws_iam_policy.deployment_dev_permissions` (name unchanged, 14 non-networking statements) and the new `aws_iam_policy.deployment_dev_networking_permissions` (12 networking statements), with a new `aws_iam_role_policy_attachment.deployment_dev_networking_permissions`. No statement merged, dropped, or semantically altered — only redistributed. Each `aws_iam_policy` resource now carries a `lifecycle.precondition` computing its own document's real Terraform-rendered JSON length against the 6,144 quota, failing `plan`/`apply` loudly (with the real character count) if still oversized — a real measurement, not an estimate. Four new `outputs.tf` outputs expose both policy ARNs and lengths. Tag enforcement, region restriction, `iam:PassRole` scoping, the RunInstances split, and every existing condition preserved unchanged. **Superseded for plan-shape purposes by the second size-quota-failure item below** — the "14 non-networking statements" and `Plan: 2 to add, 1 to change, 0 to destroy.` described here reflect the intermediate state, not the current source.
- [x] **SECOND deployment-role managed-policy size-quota failure — CAUGHT AT PLAN TIME BY THE PRECONDITION ITSELF, SPLIT INTO A THIRD POLICY (2026-07-26, reported by the user); RE-APPLIED FOR REAL AND CONFIRMED STABLE (2026-07-26, see final completion item below).** After the two-policy split above, `aws_iam_policy.deployment_dev_permissions`'s own `lifecycle.precondition` reported: `local.deployment_dev_permissions_json_length = 6212`, `local.iam_managed_policy_size_quota = 6144` — 68 characters over. **This was caught entirely at `terraform plan` time by the precondition mechanism working exactly as designed — no `iam:CreatePolicyVersion` call was made, no AWS CLI command was run, and no AWS change occurred.** The networking policy split had already resolved its own size question; only the remaining non-networking `deployment_dev_permissions` document (still 14 statements) was oversized. **Corrected**: the 4 workstation-IAM-specific statements (`DevWorkstationRoleManage`, `DevWorkstationRolePolicyAttachApprovedOnly`, `DevWorkstationInstanceProfileManage`, `DevPassWorkstationRoleToEC2Only`) moved out into a new, third data source/policy — `data.aws_iam_policy_document.deployment_dev_workstation_iam_permissions` / `aws_iam_policy.deployment_dev_workstation_iam_permissions` (name `enterprise-data-platform-dev-workstation-iam-scope-policy`), attached via a new `aws_iam_role_policy_attachment.deployment_dev_workstation_iam_permissions`. `deployment_dev_permissions` is now 10 statements; `deployment_dev_networking_permissions` (12 statements) is entirely untouched. 10 + 12 + 4 = 26, the same 26 statements as before, redistributed across three policies now — none merged, dropped, or semantically altered. The new policy carries the same `lifecycle.precondition` pattern. Two new `outputs.tf` outputs expose the third policy's ARN and computed length. Tag enforcement, region restriction, `iam:PassRole` scoping (now in the workstation-IAM policy, condition unchanged), and every existing condition preserved unchanged. Verified via Python/hcl2: 10/4/12 statements with correct Sids, 3 `aws_iam_policy` resources, 3 attachments, all files parse cleanly. **Expected next plan: `Plan: 4 to add, 1 to change, 0 to destroy.`** (existing `deployment_dev_permissions` changed in place; `deployment_dev_networking_permissions` + attachment added; `deployment_dev_workstation_iam_permissions` + attachment added). `Bootstrap_Update_1_Execution_Checklist.md` updated (fourth status note; Section 2 stop conditions/plan-review/size check for three policies; Section 3 apply-verification for three policies/attachments; Rollback section's two size bullets). **Superseded for plan-shape purposes by the forced-replacement item below** — this item's `Plan: 4 to add, 1 to change, 0 to destroy.` forecast was correct in principle but was not yet confirmed against a real plan; the real plan run against exactly this split is what surfaced the forced-replacement defect described next.
- [x] **Real, reviewed `terraform plan` against the three-policy split found an unexpected forced replacement, FIXED via `lifecycle.ignore_changes` (2026-07-26, reported by the user); RE-APPLIED FOR REAL AND CONFIRMED STABLE (2026-07-26, see final completion item below).** `terraform plan` against the three-policy split showed `Plan: 6 to add, 1 to change, 2 to destroy.` — **caught by manual review before any apply; the 2 destroys were not expected.** **Root cause**: `description` is a `Forces new resource` argument on `aws_iam_policy` in the AWS provider (AWS IAM has no API to update a customer-managed policy's description after creation). `aws_iam_policy.deployment_dev_permissions` was already applied for real, and both prior size-quota corrections had rewritten its `description` to narrate what had just changed — each rewrite silently queued a forced replacement of this already-live policy (and its attachment, as a cascading consequence, since the attachment's `policy_arn` changes when the policy is replaced). **Corrected**: `description` reset to a short, stable, split-narrative-free string; `lifecycle.ignore_changes = ["description"]` added to `aws_iam_policy.deployment_dev_permissions`, so Terraform no longer compares this argument to configuration at all — whatever description is already live in AWS stays live, permanently preventing this class of mistake. `name` and `path` were never touched by any correction. Verified via Python/hcl2: `main.tf` parses cleanly, `ignore_changes` present alongside the existing `precondition` on exactly this one resource, statement counts unchanged (10/12/4 = 26). **Expected plan reverts to `Plan: 4 to add, 1 to change, 0 to destroy.`, with zero destroys.** **No manual cleanup, state manipulation, or import was performed** — no AWS resource was affected; the unwanted destroys were caught in a reviewed `plan`, never an `apply`. `Bootstrap_Update_1_Execution_Checklist.md` updated (fifth status note; Section 2 new stop-condition bullet for any destroy/`# forces replacement`; plan-review bullet for the `ignore_changes` check; Section 3 apply-verification now also confirms `PolicyId`/`CreateDate` unchanged). Not yet re-planned or applied.
- [x] **Real, partial-destructive apply + IAM description-length validation failure — FOUND AND FIXED (2026-07-26, reported by the user).** A real `terraform apply` against the three-policy split detached and deleted the live `deployment_dev_permissions` policy, created and attached the new networking and workstation-IAM policies, then failed to recreate `deployment_dev_permissions` with a real AWS `ValidationError`: `description` exceeded IAM's 1,000-character hard limit. AWS briefly held only two of the three intended policies. **Corrected**: all three policies' `description` arguments shortened to concise, stable strings well under the limit ("Core deployment permissions for the Enterprise Data Platform.", "Networking permissions for the Enterprise Data Platform.", "Workstation IAM permissions for the Enterprise Data Platform."). Source-only correction at the time; documentation was deliberately not updated in that task per explicit instruction — closed by this catch-up entry.
- [x] **Remaining forced-replacement risk closed on all three policies (2026-07-26, reported by the user).** `lifecycle.ignore_changes = [description]` added to the two remaining policies (`deployment_dev_networking_permissions`, `deployment_dev_workstation_iam_permissions`), matching the structure already on `deployment_dev_permissions`. `precondition` blocks, policy JSON, statements, names, and description text left untouched; statement counts confirmed unchanged (10/12/4 = 26). Source-only correction at the time; documentation was deliberately not updated in that task per explicit instruction — closed by this catch-up entry.
- [x] **Bootstrap Update 1 — FINAL, STABLE RE-APPLICATION CONFIRMED (2026-07-26, reported by the user). This is the highest tier of evidence obtained for this policy: a real, successful apply plus a real, independent no-drift plan.** Following the description-length fix and the remaining `ignore_changes` additions above, a real `terraform apply` completed successfully, and a subsequent, fresh `terraform plan` reported **`No changes. Your infrastructure matches the configuration.`** All three managed policies — `enterprise-data-platform-dev-deployment-scope-policy`, `enterprise-data-platform-dev-networking-scope-policy`, `enterprise-data-platform-dev-workstation-iam-scope-policy` — are confirmed attached to the shared deployment role in real AWS, exactly matching source. **Bootstrap Update 1 is now the stable project baseline. Bootstrap Update 2 is the next implementation stage** (after `environments/dev`'s own recovery sequence, tracked separately below, is run against this now-stable permission set).
- [ ] Deployment-role trust update / Bootstrap Update 2 (IAM Sequencing Stage C / Stage-Separation-table Stage B) — not started, not coded; depends on Stage B (EC2/workstation-role creation) completing first, producing a real ARN to add.
- [x] **`environments/dev` Terraform source code — CREATED (2026-07-26).** `modules/vpc/` (`versions.tf`, `variables.tf`, `main.tf`, `outputs.tf`, `README.md`), `modules/iam-workstation-role/` (same five files), `modules/ec2-workstation/` (same five files), `environments/dev/` (`versions.tf`, `backend.tf`, `providers.tf`, `variables.tf`, `locals.tf`, `main.tf`, `outputs.tf`, `terraform.tfvars.example`, `backend.hcl.example`, `README.md`), and `infrastructure/terraform/scripts/bootstrap_workstation.sh` all exist on disk, matching the approved plan's §7 file list exactly (plus `backend.tf`, called for by §7's directory tree though omitted from this task's own abbreviated file list). **Code evidence (Tier 2) only** — see the unchecked items below for everything this does NOT cover.
- [x] **`environments/dev` real, private `backend.hcl`/`terraform.tfvars` — CREATED (2026-07-26).** Both gitignored (confirmed against the existing `.gitignore` filename patterns, no `.gitignore` change needed); neither `.example` template was modified. `terraform.tfvars` populates the two required-no-default variables (`aws_account_id`, `deployment_role_arn` — sourced from the real, already-existing `bootstrap/terraform.tfvars` and the deployment role's already-documented name, not from `terraform output`) plus `region`/`project_name`/`environment`/`vpc_cidr`/`public_subnet_cidr`/`instance_type` at their already-approved values; no workstation-role-naming variable exists in `environments/dev/variables.tf` to populate (confirmed, none added); no credential or CLI profile added. `backend.hcl` populates the real state bucket name, `key = "dev/terraform.tfstate"`, `region`/`encrypt`/`use_lockfile`, and a nested `assume_role` block (same deployment role ARN, `session_name = "terraform-dev-backend"`). **This is file creation only — `environments/dev` has NOT been initialized against this backend, no Bootstrap Update 1 command has been run, and no `plan`/`apply` has occurred.** A companion `16_Implementation_Notes/Bootstrap_Update_1_Execution_Checklist.md` was created the same day: an exact, ordered, six-section sequence (pre-checks; Bootstrap Update 1 plan-only with stop conditions; Bootstrap Update 1 apply gated on explicit approval, with AWS-side verification; dev backend initialization with no-migration/lock checks; the first `environments/dev` plan with itemized resource verification; consolidated rollback/stop-conditions) for actually running the steps below. No real value is reproduced in either tracking document.
- [~] **`environments/dev`/`bootstrap` first validation gate — ATTEMPTED (2026-07-26), BLOCKED by sandbox tooling unavailability.** No `terraform` binary, no `tflint`, no `trivy` exist in this Cowork sandbox; no root/`apt` access to install them; `releases.hashicorp.com` and GitHub's release-asset CDN are both blocked by the network allowlist. **`terraform fmt`/`init`/`validate`, `tflint`, and Trivy were NOT run — none of these is marked complete.** In their place: a `pip`-installed Python `hcl2` parser confirmed all 26 `.tf` files are syntactically valid HCL2 (real, automated, but not equivalent to `terraform validate`'s schema-aware checking); `bash -n` confirmed the bootstrap script is syntactically valid Bash without executing it; a heuristic scan found and corrected 7 files' worth of `terraform fmt`-style misalignment (44 lines) plus a pre-existing typo in 5 files; and a full manual review covered the IAM policy, backend/provider config, the bootstrap script, and general source hygiene against the task's complete checklist. Two genuine findings recorded, not silently fixed: a curl-pipe-shell pattern installing `uv` (no checksum/pin), and an IAM statement permitting untagged dev-networking resource creation (matches the plan's own already-disclosed §11.6 residual risk).
- [x] **`environments/dev`/module `terraform fmt -check -recursive` — run for real (2026-07-26) on the user's Windows machine and PASSED.** No file needed reformatting.
- [x] **`environments/dev`/`bootstrap`/module `terraform validate` — run for real (2026-07-26) on the user's Windows machine and PASSED in all five directories:** `bootstrap/`, `environments/dev/`, `modules/vpc/`, `modules/iam-workstation-role/`, `modules/ec2-workstation/`. This is confirmed Tier 4 (Terraform validation) evidence, superseding the sandbox-blocked attempt above for `fmt`/`validate` specifically.
- [x] **TFLint — re-run for real (2026-07-26) on the user's Windows machine (`tflint --recursive`); COMPLETED WITH ZERO FINDINGS.** Confirms the prior nine fixes (1-2: two unused reserved-CIDR variables removed from `environments/dev/variables.tf`; 3: the unused `project_name` variable removed from `modules/iam-workstation-role/variables.tf` and its argument removed from `environments/dev/main.tf`; 4-9: `required_version`/AWS-provider version constraints added to all three child modules) are correct and complete, with no new finding introduced. This is now confirmed, re-validated Tier 4 (Terraform validation) evidence.
- [x] **Trivy config scan — RUN FOR THE FIRST TIME (2026-07-26), Trivy v0.72.0, on the user's Windows machine.** Five findings, all reviewed and dispositioned (`Dev_Environment_Terraform_Implementation_Plan.md` §42b): `AWS-0089` LOW (bootstrap, S3 access logging disabled) — accepted, unchanged from bootstrap's original disposition; `AWS-0132` HIGH (bootstrap, no customer-managed KMS key) — accepted approved design decision, SSE-S3 intentional; `AWS-0342` MEDIUM (bootstrap, `iam:PassRole` present) — accepted after re-confirming exact workstation-role-ARN scoping and `iam:PassedToService = ec2.amazonaws.com`, no wildcard; `AWS-0104` CRITICAL (dev/modules, unrestricted security-group egress) — accepted as a temporary, explicitly risk-accepted dev-phase exception (zero inbound remains enforced; future tightening options recorded); `AWS-0178` MEDIUM (dev/modules, VPC Flow Logs disabled) — accepted as a deferred control for a single disposable dev workstation. `AWS-0104`/`AWS-0178` were each reported twice (via `environments/dev/` and via the modules scanned directly) and consolidated to one disposition each, not treated as four findings. **No `.trivyignore` or inline suppression added.** **Not fully input-aware** — Trivy warned variable values were unavailable (no `--tf-vars` supplied); a future scan should use the real, local, gitignored `tfvars` files, values never reproduced in documentation.
- [x] Child-module `.terraform.lock.hcl` cleanup — the standalone `terraform init`/`validate` runs against `modules/vpc/`, `modules/iam-workstation-role/`, and `modules/ec2-workstation/` as isolated directories each generated their own `.terraform.lock.hcl` (and `.terraform/`). These are validation-only artifacts and must **not** be committed — only `bootstrap/.terraform.lock.hcl` and `environments/dev/.terraform.lock.hcl` are committed lock files (`Dev_Environment_Terraform_Implementation_Plan.md` §40, revised 2026-07-26). `.gitignore` now excludes `infrastructure/terraform/modules/*/.terraform.lock.hcl`. Manual deletion of the three module-directory lock files (and each module's `.terraform/`) remains a required, not-yet-performed local action.
- [~] **IAM policy tag-enforcement bypass — FOUND AND CORRECTED (2026-07-26), NOT YET RE-VALIDATED.** `bootstrap/main.tf`'s Bootstrap Update 1 policy had an unconditioned `DevNetworkingCreateManage` statement granting 5 EC2-networking create actions with no tag requirement, alongside `DevNetworkingCreateManageTaggedOnCreate`'s tagged grant of those same 5 actions — since IAM Allow statements are additive, the untagged grant made the tag condition unenforceable, a real bypass. Fixed by splitting into `DevNetworkingCreateTaggedOnly` (create actions, tag-on-create required, no untagged alternative exists) and `DevNetworkingManageTaggedResourceOnly` (remaining manage actions, conditioned on the target resource already carrying the required tags) — condition-key support independently confirmed against AWS's own EC2 IAM reference for 11 of 15 "manage" actions; the remaining 7 flagged as pattern-inferred, pending confirmation. `DevReadOnlyDescribe` gained an `aws:RequestedRegion` condition. No broader permission, bootstrap-state access, or deployment-role self-management introduced. **This correction was made after the TFLint/Trivy runs above and has not yet been re-validated by a real `fmt`/`validate`/`tflint`/Trivy pass.**
- [~] **`DevRunInstancesAmi` authorization investigation — AUTHENTICATION ROOT CAUSE RESOLVED; BOOTSTRAP IAM-CONDITION DEPLOYMENT AND FUTURE-LAUNCH AUTHORIZATION VALIDATION REMAIN PENDING (2026-07-26). NOT fully closed.** Real, repeated `ec2:RunInstances UnauthorizedOperation`/`matchedStatements: []` failures against the AMI resource, despite `DevRunInstancesAmi` appearing correct on static review, led to a one-variable-at-a-time diagnostic investigation (temporarily removing `ec2:Owner`, then `ec2:InstanceType`, then `aws:RequestedRegion`). Two false claims about missing permissions (no EC2 compute permissions granted; no S3 state/lock access policy exists) were raised and **corrected via direct re-verification against source rather than complied with** — no new managed policy was created either time. **Real root cause**: an MFA/STS credential-flow issue meant Terraform was not genuinely operating as the deployment-role session throughout the investigation; once resolved, `RunInstances` succeeded even with `DevRunInstancesAmi` stripped bare, ruling out all three conditions as the actual blocker — this diagnostic conclusion is verified. **What remains open**: whether the conditions later restored in source (below) have actually been applied to the real, deployed IAM policy, and whether a future, genuinely new `RunInstances` request succeeds under them — see the two items below and `PROJECT_EXECUTION_JOURNAL.md` Section 27af for the required next steps. Full record: Section 27aa (and its correction in 27ac).
- [x] **Free Tier instance-type restriction — REAL, ACCOUNT-SPECIFIC, WORKED AROUND TEMPORARILY (2026-07-26).** This AWS account rejects `t3.medium`/`t3.large`/`t3.xlarge` at `RunInstances` with a real `InvalidParameterCombination` Free-Tier-eligibility error; manual testing confirmed `t3.small`/`t3.micro`/`m7i-flex.large` launch successfully. Per explicit instruction not to replace `t3.medium` project-wide or change AWS accounts, `t3.small` was added to the `instance_type` allow-list in `environments/dev/variables.tf`, `modules/ec2-workstation/variables.tf`, and `DevRunInstancesSupportingResources`'s `ec2:InstanceType` condition — approved default/design target remains `t3.medium`, exact revert condition documented in all three files. Full record in `PROJECT_EXECUTION_JOURNAL.md` Section 27ab.
- [~] **`DevRunInstancesAmi` conditions restored IN SOURCE ONLY — NOT YET CONFIRMED DEPLOYED (2026-07-26).** `aws:RequestedRegion` and `ec2:Owner = amazon` restored in `bootstrap/main.tf` exactly as originally designed; `ec2:InstanceType` restored with `t3.small` deliberately included for consistency with the workaround above. Verified via a Python HCL structure check (187/187 braces, 29 statement blocks, 60 condition blocks). **A `bootstrap/` plan/apply has NOT been run since this restoration** — whether the deployed deployment-role policy actually contains these three conditions is not yet confirmed. Full record in `PROJECT_EXECUTION_JOURNAL.md` Section 27ac (including its correction).
- [x] **`environments/dev`'s own state matches its configuration — REAL `terraform plan`/`apply` SUCCEEDED (2026-07-26).** Run from `environments/dev` on the user's Windows machine, after a fresh MFA login: `terraform plan` and `terraform apply` both reported **`No changes. Your infrastructure matches the configuration.`** / **`Apply complete! Resources: 0 added, 0 changed, 0 destroyed.`** — confirming the VPC, subnet, Internet Gateway, route table (+ association), security group, workstation IAM role/instance-profile/attachment, and the EC2 workstation itself all still match `environments/dev`'s Terraform state and configuration. **This does NOT verify the `DevRunInstancesAmi` restoration above** — `environments/dev` and `bootstrap/` are separate Terraform stacks with separate state; this apply never reads or applies anything in `bootstrap/main.tf`. The EC2 workstation visible here was originally launched while `DevRunInstancesAmi` was still stripped bare for diagnostics (item above), so its existing, already-successful launch is not evidence about the restored, currently-undeployed conditions either. Full record in `PROJECT_EXECUTION_JOURNAL.md` Section 27ac/27af.
- [x] **`environments/dev` `terraform plan`/`apply` — no longer partial; the item above supersedes the two earlier partial-apply entries.** Neither `dev_initial.tfplan` nor `dev_recovery.tfplan` was reused; the verified apply above used a freshly generated plan against `environments/dev`'s current source.
- [x] **Self-assume-role recurrence — DIAGNOSED AND CLOSED, NOT WORKED AROUND (2026-07-26).** A day after the verified apply above, a fresh `environments/dev apply` failed with a self-assume `AccessDenied` because the ambient shell already held an assumed deployment-role session (session name `terraform-dev`, matching neither of this project's own two Terraform-driven session names) when `providers.tf`'s own `assume_role` block tried to assume the same role again. Diagnosed against `bootstrap/main.tf`'s real trust policy (trusts only the IAM user, not an assumed-role session) rather than fixed by removing `providers.tf`'s `assume_role` block. **`login.ps1` (project root) reviewed and confirmed to already implement the required corrected workflow** — clears stale session-credential environment variables, requests a fresh MFA session via `aws sts get-session-token` (never `assume-role`), stops on STS failure without printing success, sets region variables, confirms success only via a real `aws sts get-caller-identity`. No change to `login.ps1` was needed. Full record in `PROJECT_EXECUTION_JOURNAL.md` Section 27ad.
- [x] **Final, repeatable `environments/dev` Terraform workflow — DOCUMENTED (2026-07-26).** `.\login.ps1` from the project root → enter MFA → confirm identity is the base IAM user → `cd infrastructure\terraform\environments\dev` → `terraform plan` → `terraform apply` → never manually run `aws sts assume-role` beforehand. Full record in `PROJECT_EXECUTION_JOURNAL.md` Section 27ae.
- [x] **Connect with Session Manager — CONFIRMED (2026-08-04), via the real bootstrap-script execution below.** No standalone connectivity test was run in isolation, but the bootstrap script's real, successful execution as `ssm-user` (the account's actual Session Manager interactive user, not AL2023's default `ec2-user`) on the real EC2 workstation is itself direct evidence that a Session Manager session was established and could run commands with `sudo` — this supersedes the "not yet run" status previously recorded here.
- [ ] Native S3 locking deliberate contention test (scheduled for the `environments/dev` apply window, per the bootstrap-phase deferral) — not run; the verified apply above was a no-change reconciliation, not a genuinely long-running lock-holding operation.
- [~] **Install Git, Python, Terraform, Docker, AWS CLI, dbt — PARTIALLY CONFIRMED (2026-08-04).** Verified installed and working on the real EC2 workstation: Git, Python, Terraform (v1.15.8), AWS CLI. **Not yet independently verified**: Docker, dbt — left open, not marked complete.
- [ ] Configure shutdown — automatic shutdown or a disciplined stop process for the workstation has not been configured or evidenced; remains open (Pre-Phase §10 Step 4).
- [x] Push repository — the GitHub repository exists (item above, confirmed earlier in this project).
- [ ] ~~Begin Phase 0~~ — **superseded, see the Pre-Phase closure item below.** Phase 0 — AWS Platform Foundation has not begun; the next task is Phase 0's formal phase-entry review, not Phase 0 work itself.
- [x] **Pre-Phase — Engineering Environment Setup — COMPLETED AND VALIDATED ON REAL AWS (2026-08-04).** A final `aws ec2 run-instances --dry-run` under the confirmed shared deployment-role session returned `DryRunOperation: Request would have succeeded, but DryRun flag is set.` — no EC2 resource created — closing the last open IAM validation item. Full completion evidence: MFA-backed IAM user login works; Terraform assumes the shared deployment role; bootstrap IAM policies deployed; Bootstrap Update 2 trust policy deployed and validated; EC2 workstation reachable through Session Manager; the workstation instance role can assume the shared deployment role; workstation bootstrap v1.1.1 completed on Amazon Linux 2023; Git/GitHub CLI/Terraform 1.15.8/`uv`/AWS CLI/Python/`jq` verified; the `DevRunInstancesAmi` policy defect corrected. **This is Pre-Phase completion per `PROJECT_BLUEPRINT.md` §10, checked against the blueprint before recording — not Phase 0 completion.** Phase 0 — AWS Platform Foundation (§11: CloudTrail/CloudWatch, KMS, Secrets Manager, Budgets-in-Terraform, GitHub Actions CI/CD, VPC endpoints/Flow Logs, broader IAM foundation, multi-environment groundwork, recovery/recreation testing) has not begun and is not claimed complete. Also still open, non-blocking: Docker/dbt/Java/PostgreSQL client/`curl`/`make`/`tmux`/Ruff/Pytest verification, VS Code remote development, and workstation shutdown automation (items above). Full record: `PROJECT_EXECUTION_JOURNAL.md` Section 27ak. **Next task: Phase 0 — AWS Platform Foundation: perform the formal phase-entry review against `PROJECT_BLUEPRINT.md` §11 and identify the first approved implementation workstream.**
- [x] **Bootstrap IAM conditions — DEPLOYED SUCCESSFULLY (2026-07-26).** Real `terraform apply` against the `bootstrap/` stack: `Apply complete! Resources: 0 added, 1 changed, 0 destroyed.` — the deployment role's managed policy now carries the restored `DevRunInstancesAmi` conditions in real AWS, confirmed. Full record: `PROJECT_EXECUTION_JOURNAL.md` Section 27ag.
- [x] **Future EC2 launch authorization validation for the deployed `DevRunInstancesAmi` conditions — COMPLETE (2026-08-04).** An earlier real `--dry-run` test found a real defect: `DevRunInstancesAmi`'s `ec2:InstanceType` condition (an instance-resource key, not applicable to the AMI resource type) caused the entire statement not to match under `StringEquals`, denying `UnauthorizedOperation` on the AMI resource. **Corrected** in `bootstrap/main.tf` — that condition removed from `DevRunInstancesAmi`; `ec2:Owner`/`aws:RequestedRegion` unchanged; `ec2:InstanceType` remains fully enforced on `DevRunInstancesSupportingResources`. **A fresh, final `aws ec2 run-instances --dry-run` under the confirmed deployment-role session against the approved launch path then returned `DryRunOperation: Request would have succeeded, but DryRun flag is set.`** — no EC2 resource created, confirming the fix and closing this item. This also corrects two prior entries in this checklist and elsewhere in this project's record: the bootstrap IAM-condition deployment being described as closing this investigation, and any prior suggestion that `ec2:InstanceType` on the AMI statement was harmless — neither was accurate. **This was the only remaining IAM-related validation item in this project; it is now closed.** Full record: `PROJECT_EXECUTION_JOURNAL.md` Sections 27aj/27ak.
- [x] **Bootstrap Update 2 completed and validated (2026-08-04). The EC2 workstation role can successfully assume the shared deployment role.** `bootstrap/main.tf`'s `deployment_role_trust` extended with a second statement (`AllowDevWorkstationRoleAssumeRoleNoMfa`, principal `local.dev_workstation_role_arn`, no MFA condition), keeping the existing human-MFA statement (`AllowHumanBootstrapPrincipalAssumeRoleWithMFA`) unchanged. On the user's own machine: `terraform fmt`, `terraform validate`, and `terraform plan` all ran for real; the saved plan was reviewed and confirmed the human principal's MFA condition was preserved and the workstation-role trust was added as a fully separate statement; `terraform apply` completed with `Apply complete! Resources: 0 added, 1 changed, 0 destroyed.` On the real EC2 workstation, via Session Manager, `aws sts get-caller-identity` confirmed the workstation instance role, and that role successfully assumed the shared deployment role — a real functional test of the new trust path. Full record: `PROJECT_EXECUTION_JOURNAL.md` Section 27ai.
- [x] **`bootstrap_workstation.sh` v1.1.1 — RUN FOR REAL AND COMPLETED SUCCESSFULLY on the real EC2 workstation, Amazon Linux 2023 (2026-08-04).** The user reported and this checklist records the following as verified on the real instance: AWS CLI available (preinstalled by the AMI, not this script); Git installed; GitHub CLI installed (install-only, not authenticated, per §29.2 item 2); Terraform v1.15.8 installed from the official HashiCorp release archive with SHA256 checksum verification, to `/usr/local/bin`; `uv` installed for `ssm-user` specifically (not root); the `projects` working directory created under `/home/ssm-user/projects` with correct ownership; the bootstrap version marker (`/etc/bootstrap_workstation_version`) set to `1.1.1`, matching the script's current version after the `curl`/`curl-minimal` fix. This is the first real-instance execution evidence for this script — prior entries (v1.0.0 authoring, v1.1.0 user/Terraform revision, v1.1.1 curl-conflict fix) were source/`bash -n`-only. **Not separately re-confirmed by this entry:** `gh` remaining unauthenticated, no credential/AWS-key file written, no repository cloned, no Terraform command executed by the script itself — these remain governed by the script's own unchanged prohibitions (§29.2) and were not independently re-verified command-by-command against this run; nothing in the user's report contradicts them. Per §56.1 of `02_Infrastructure/EC2_Development_Workstation.md`, this real, successful bootstrap execution is the acceptance-criteria evidence that item required — the workstation can now be treated as fully accepted on that basis. Full record: `PROJECT_EXECUTION_JOURNAL.md` Section 27ah.
